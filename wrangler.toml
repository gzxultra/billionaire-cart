name = "billionaire-cart"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = ".vercel/output/static"

# D1 database — uncomment after creating via: wrangler d1 create billionaire-cart-db
# Then replace database_id with the real UUID
# [[d1_databases]]
# binding = "DB"
# database_name = "billionaire-cart-db"
# database_id = "REPLACE_WITH_REAL_UUID"
# migrations_dir = "migrations"
